import { HomeRouting.Module } from './home-routing.module';

describe('HomeRouting.Module', () => {
  it('should create an instance', () => {
    expect(new HomeRouting.Module()).toBeTruthy();
  });
});
